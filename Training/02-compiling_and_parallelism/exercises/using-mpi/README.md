# Building and running MPI program

Build the provided Fortran MPI program `mpi-hello.F90", *e.g.* as
```
mpif90 -o hello mpi-hello.F90
```

Run the resulting executable `hello` via the batch job system
